[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Send every route to index.html so React Router handles it
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
